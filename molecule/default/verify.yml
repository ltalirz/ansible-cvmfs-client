- name: Verify
  hosts: all
  become: true

  tasks:
  - name: CVMFS check setup
    command: cvmfs_config chksetup

  - name: CVMFS probe
    command: cvmfs_config probe

  - name: Try enabling Compute Canda module environment
    shell: source /cvmfs/soft.computecanada.ca/config/profile/bash.sh